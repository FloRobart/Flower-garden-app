
name: Build and publish Docker images

on:
  push:
    branches:
      - master

permissions:
  contents: read
  packages: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set lowercase repository owner
        id: repo_owner_lc
        run: echo "repo_owner_lc=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT

      - name: Set lowercase repository name
        id: repo_name_lc
        run: |
          repo_name="${GITHUB_REPOSITORY##*/}"
          echo "repo_name_lc=${repo_name,,}" >> $GITHUB_OUTPUT

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm ci

      - name: Get package version
        id: vars
        run: |
          version=$(node -p "require('./package.json').version")
          echo "version=$version" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Connexion à Docker Hub (si credentials présents)
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Connexion à GitHub Container Registry
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # --- Docker Hub Builds ---
      - name: Build and push ${{ steps.repo_name_lc.outputs.repo_name_lc }}-server (Docker Hub)
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.repo_name_lc.outputs.repo_name_lc }}-server:${{ steps.vars.outputs.version }}
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.repo_name_lc.outputs.repo_name_lc }}-server:latest

      # --- GHCR Builds ---
      - name: Build and push ${{ steps.repo_name_lc.outputs.repo_name_lc }}-server (GHCR)
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.repo_owner_lc.outputs.repo_owner_lc }}/${{ steps.repo_name_lc.outputs.repo_name_lc }}-server:${{ steps.vars.outputs.version }}
            ghcr.io/${{ steps.repo_owner_lc.outputs.repo_owner_lc }}/${{ steps.repo_name_lc.outputs.repo_name_lc }}-server:latest